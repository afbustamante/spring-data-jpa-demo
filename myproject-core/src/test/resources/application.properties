# Spring profiles
spring.profiles.active=test

# Datasource
spring.datasource.url=jdbc:hsqldb:mem:demo-test
spring.datasource.username=sys
spring.datasource.password=sys
spring.datasource.driver-class-name=org.hsqldb.jdbc.JDBCDriver

# Spring Data / JPA
spring.jpa.database-platform=org.hibernate.dialect.HSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

# Liquibase
spring.liquibase.enabled=true
spring.liquibase.url=jdbc:hsqldb:mem:demo-test
spring.liquibase.user=sys
spring.liquibase.password=sys
spring.liquibase.driver-class-name=org.hsqldb.jdbc.JDBCDriver
